# Database (shared with packages/db Docker Compose)
DATABASE_URL="postgres://exort:exort@localhost:5432/exort"

# SvelteKit origin
ORIGIN="http://localhost:5173"

# Better Auth (must match apps/api secret)
# For production use 32 characters and generated with high entropy
BETTER_AUTH_SECRET=""

# Lichess OAuth (no registration needed — Lichess uses PKCE public clients)
LICHESS_CLIENT_ID="exort"
LICHESS_CLIENT_SECRET=""
